{
  "name": "newsdigest-backend",
  "version": "1.0.0",
  "private": true,
  "main": "worker/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:fe": "cd fe && npm run dev",
    "deploy": "npm run deploy:all",
    "deploy:worker": "wrangler deploy",
    "deploy:fe": "cd fe && npm run build && set -a && source ../.env && set +a && npx wrangler pages deploy build --project-name=newsdigest-fe --commit-dirty=true",
    "deploy:all": "npm run deploy:worker && npm run deploy:fe"
  },
  "dependencies": {
    "fast-xml-parser": "^4.3.6",
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.0"
  }
}
